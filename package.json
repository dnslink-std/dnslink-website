{
  "name": "dnslink.dev",
  "version": "1.0.0",
  "description": "dnslink website",
  "main": "index.js",
  "dependencies": {
    "remarkable": "^2.0.1"
  },
  "devDependencies": {
    "markdown-toc": "^1.2.0",
    "marked": "^2.1.1",
    "mustache": "^4.2.0",
    "serve": "12.0.0",
    "shx": "0.3.3"
  },
  "scripts": {
    "start": "npm run build && serve -l 3000 ./build",
    "build": "shx rm -fr build && shx mkdir build && node index.js > build/index.html && shx cp -r img build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jbenet",
  "license": "MIT"
}
